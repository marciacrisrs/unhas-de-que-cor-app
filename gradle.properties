# Project-wide Gradle settings.
# IDE (e.g. Android Studio) users:
# Gradle settings configured through the IDE will override
# any settings specified in this file.
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
android.nonTransitiveRClass=true
# The Kotlin source set model now uses the supported Android Gradle Plugin default.
# Configuration cache is disabled because the model download task performs
# an external artifact download and is intentionally executed outside the cache.
org.gradle.configuration-cache=false
# Kotlin code style for this project: "official" or "obsolete":
kotlin.code.style=official

# SonarCloud (opcional local — prefira env SONAR_* ; ver docs/sonar.md)
# sonar.organization=sua-org
# sonar.projectKey=sua-org_unhas-de-que-cor-app
# sonar.host.url=https://sonarcloud.io
